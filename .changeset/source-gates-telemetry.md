---
'docstube': patch
---

Ground generation in codemap source facts, persist agent cache/transcripts, run the expanded deterministic page gate, honor `caps.maxPages`, tighten `check --all`, pass GitHub Action config paths, and emit privacy-limited runtime telemetry when configured.

