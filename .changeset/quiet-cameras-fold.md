---
"docstube": patch
---

Add the first usable CLI command surface for the wizard, generation, refresh, refinement, validation, checks, status, doctor, upgrade, help, and version paths. The upgrade path detects source checkouts, package-manager installs, ephemeral runs, and standalone binaries.
