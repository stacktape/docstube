<!--
Variant 02 — "Verified, always-current documentation — generated from your code."
Lead angle: The founder's alt headline — the clean category definition that sidesteps the buzzword. Tri-claim up front; autonomy lives in the subhead.
Tone: Polished/premium, Linear/Stripe register. Calm, confident, declarative.
Primary persona: Serious evaluators and teams comparing real options.
Why it might win: Defines the category in plain language without leaning on "AI-native"; reads like a finished product, not a pitch.
Biggest risk: Without the buzzword contrast it can feel generic — the tri-claim and the gate must do the differentiating.
-->

## Hero

**H1 —** Verified, always-current documentation — generated from your code.

**Sub —** Answer a few questions, then let it run. docstube reads your codebase, writes your docs, fact-checks every claim against the source, and keeps everything in sync as your code changes. Open source — and it runs on the AI you already pay for.

**Primary CTA —** Get started: `npx docstube wizard`

**Secondary CTA —** See the verification gate

**Hero visual —** A clean, premium docs site rendered in the docstube theme, with three quiet status badges along the top of one page: "Samples compiled," "Links resolved," "Reviewed for: API consumer." A subtle "in sync with `main`" marker sits beside the page title.

## Why docstube

- **Verified, not hallucinated.** Every factual claim is grounded in your source and machine-checked before it ships.
- **Always-current.** Symbol-level provenance means only affected pages regenerate on a code change — locally or via a GitHub Action PR.
- **Generated from your code.** A writer agent drafts from real source; nothing is invented at the keyboard.
- **Written for your readers.** Name your personas; each page is reviewed for the right depth and tone.
- **Bring your own AI.** Runs on your existing Claude, Codex, or Gemini subscription, or any compatible API key, with spend caps you control.
- **Beautiful & modern.** A custom Astro + React theme with hosted-platform polish, themeable to your brand.
- **Open source.** MIT, generates MDX, zero lock-in.

## Use-cases

- **Public docs sites** — clean structure for SEO and AEO, built-in Pagefind search, llms.txt, and an MCP server, on a site you fully own.
- **Internal docs & wikis** — onboarding, architecture, and tribal knowledge for private repos, on your own infra and keys.
- **Better LLM context** — verified llms.txt and llms-full.txt plus an MCP server, so agents read checked context rather than concatenated source.

## How it works

1. Point docstube at your repo. A local web UI wizard opens.
2. Choose the doc type, define reader personas, add a little context.
3. A writer agent reads your code; reviewer and verifier agents check it for audience fit and accuracy.
4. Review the result rendered exactly as it'll look live, and leave feedback on any element.
5. The writer agent refines until you're happy.
6. On every code change, only the affected pages regenerate.

## The gate — why it's verified

"Always-current" and "verified" aren't adjectives here. They're enforced before a page ships.

- Deterministic checks run first: MDX compiles, component props validate, TS samples pass `tsc`, Python passes `pyright`.
- Imports resolve against the real package — phantom functions never reach the reader.
- Broken links, invalid MDX, and malformed D2 diagrams are machine-checked. They don't reach readers.
- API tables are extracted from your types with TypeDoc and griffe, not written by an LLM. Signatures change, tables update — zero tokens.
- The agent that writes the page isn't the agent that judges it. No page ships with an open blocker.

## Use your existing subscription

Run docstube on the coding agent you already pay for — the official Claude, Codex, or Gemini CLIs — or point the direct-API adapter at any OpenAI- or Anthropic-compatible endpoint, including open-weight and self-hosted models. docstube estimates usage and freezes at the cap you set, with margin, and runs are resumable, so spend stays predictable.

Your source context goes only to the AI provider you already use, on your own credentials — never to docstube's servers.

## Built for the agent era

And of course, the site ships agent-ready: llms.txt, llms-full.txt, and a docs-serving MCP server, so the agents that consume your docs get the same verified content as your readers.

## You own the output

docstube produces a self-contained Astro + React site, vendored into your repo with no runtime dependency on docstube. Theme it to your brand, eject it, host it anywhere. MIT-licensed, no lock-in.

## Footer credit

A discreet "Generated by docstube" credit ships on by default. Disable it any time with `theme.attribution: false`.

## FAQ

**How is this different from a docs tool with an AI button?** Those help a human write. docstube generates the whole site from your source and verifies it before it ships — you supervise, not author.

**What keeps it current?** A tree-sitter codemap tracks symbol-level provenance. When code changes, only the affected pages regenerate, and the GitHub Action opens a PR with just the diff.

**Does my code leave my machine?** Source context goes only to the AI provider you already use, on your own credentials — never to docstube's servers.

**Do I depend on docstube to serve the docs?** No. The output is self-contained and yours.

## Final CTA

**Headline —** Documentation that's verified, current, and generated from the code it describes.

**CTA —** `npx docstube wizard`

**Microcopy —** Open source. Runs on your own AI. Nothing ships with an open blocker.
